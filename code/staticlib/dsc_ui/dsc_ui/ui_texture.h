#pragma once
#include "dsc_ui.h"

namespace DscRender
{
	class IRenderTarget;
}

namespace DscUi
{
	class UiTexture
	{
	public:
		//create with a render target to draw ui into, or we can just use the draw system 
		UiTexture(
			);
	private:

	};
}